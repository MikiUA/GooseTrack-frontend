import { Routes } from 'react-router';
import '../index.css';

export const App = () => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
        fontFamily: 'Coolvetica',
      }}
    >
      React homework template
    </div>
  );
};
